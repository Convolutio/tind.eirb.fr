# Pocketbase backend

Important: explicitly set the timezone in the school's region:

```sh
# before each command
# in production, set the variable in the container
TZ=Europe/Paris ./pocketbase ...
``` 
